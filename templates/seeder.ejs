import { Seeder } from '@athenna/database'

export class <%= namePascal %> extends Seeder {
    /**
     * Run the database seeders.
     *
     * @return {Promise<void>}
     */
    async run() {}
}
